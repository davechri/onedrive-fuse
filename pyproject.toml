[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}

[project]
name = "onedrive-fuse"
version = "0.0.1"
license = { text = "MIT" }
dynamic = ["dependencies"]
authors = [
    { name="Dave Christenson", email="davechri58@gmail.com" }
]
description = "onedrive-fuse is a Linux or macOS filesystem for OneDrive"
readme = "README.md"
keywords = ["offline", "cache", "onedrive", "fuse-filesystem", "fuse"]
classifiers = [
  # How mature is this project? Common values are
  #   3 - Alpha
  #   4 - Beta
  #   5 - Production/Stable
  "Development Status :: 4 - Beta",

  # Indicate who your project is intended for
  "Topic :: System :: Filesystems",
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "Intended Audience :: End Users/Desktop",
  "Operating System :: MacOS :: MacOS X",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python",
  "Programming Language :: Python :: 2.7",
  "Programming Language :: Python :: 3.3",
  "Programming Language :: Python :: 3.4",
  "Programming Language :: Python :: 3.5",
  "Programming Language :: Python :: Implementation :: PyPy",
  "License :: OSI Approved :: MIT License",  
]

[project.urls]
Repository = "https://github.com/davechri/onedrive-fuse"
Issues = "https://github.com/davechri/onedrive-fuse/issues"

[project.scripts]
onedrive-fuse = "onedrive_fuse.cli:main"
